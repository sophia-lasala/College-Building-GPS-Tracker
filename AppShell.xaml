<?xml version="1.0" encoding="UTF-8"?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:GPSTrackerNYIT"
       xmlns:controls="clr-namespace:GPSTrackerNYIT.Controls"
       xmlns:sys="clr-namespace:System;assembly=netstandard"
       xmlns:views="clr-namespace:GPSTrackerNYIT.Views"
       x:Class="GPSTrackerNYIT.AppShell"
       x:Name="shell"
       FlyoutBackgroundImage="photo.jpg"
       FlyoutBackgroundImageAspect="AspectFill"
       FlyoutHeaderBehavior="CollapseOnScroll"
       x:DataType="local:AppShell">
    <Shell.Resources>
        <Style x:Key="BaseStyle"
               TargetType="Element">
            <Setter Property="Shell.BackgroundColor"
                    Value="#455A64" />
            <Setter Property="Shell.ForegroundColor"
                    Value="White" />
            <Setter Property="Shell.TitleColor"
                    Value="White" />
            <Setter Property="Shell.DisabledColor"
                    Value="#B4FFFFFF" />
            <Setter Property="Shell.UnselectedColor"
                    Value="#95FFFFFF" />
        </Style>
        <Style TargetType="ShellItem"
               BasedOn="{StaticResource BaseStyle}" />
        <Style x:Key="DomesticShell"
               TargetType="Element"
               BasedOn="{StaticResource BaseStyle}">
            <Setter Property="Shell.BackgroundColor"
                    Value="#039BE6" />
        </Style>
        <Style x:Key="MapShell"
               TargetType="Element"
               BasedOn="{StaticResource BaseStyle}">
            <Setter Property="Shell.BackgroundColor"
                    Value="#ED3B3B" />
        </Style>
        <Style x:Key="VideoShell"
               TargetType="Element"
               BasedOn="{StaticResource BaseStyle}">
            <Setter Property="Shell.BackgroundColor"
                    Value="#546DFE" />
        </Style>
        <Style x:Key="CreditShell"
               TargetType="Element"
               BasedOn="{StaticResource BaseStyle}">
            <Setter Property="Shell.BackgroundColor"
                    Value="#96d1ff" />
        </Style>
        <Style x:Key="HistoryShell"
               TargetType="Element"
               BasedOn="{StaticResource BaseStyle}">
            <Setter Property="Shell.BackgroundColor"
                    Value="#ED3B3B" />
        </Style>
    </Shell.Resources>

    <Shell.FlyoutHeader>
        <controls:FlyoutHeader />
    </Shell.FlyoutHeader>

    <Shell.FlyoutFooter>
        <controls:FlyoutFooter />
    </Shell.FlyoutFooter>

    <FlyoutItem Route="options"
                FlyoutDisplayOptions="AsMultipleItems">

        <ShellContent Route="map"
                      Style="{StaticResource MapShell}"
                      Title="Map"
                      Icon="appicon.png"
                      ContentTemplate="{DataTemplate views:MapPage}" />
        <ShellContent Route="video"
                      Style="{StaticResource VideoShell}"
                      Title="Video"
                      Icon="appicon.png"
                      ContentTemplate="{DataTemplate views:VideoPage}" />
        <ShellContent Route="credits"
                      Style="{StaticResource CreditShell}"
                      Title="Credit"
                      Icon="appicon.png"
                      ContentTemplate="{DataTemplate views:CreditPage}" />
        <ShellContent Route="history"
                      Style="{StaticResource HistoryShell}"
                      Title="History"
                      Icon="appicon.png"
                      ContentTemplate="{DataTemplate views:HistoryPage}" />
    </FlyoutItem>

    <MenuItem Text="Help"
              IconImageSource="help.png"
              Command="{Binding HelpCommand}"
              CommandParameter="https://site.nyit.edu/files/long_island/Locations_LI_CampusMap.pdf" />

</Shell>